// import { Router } from "express";
// import { TestProductController } from "../controllers/Test_produk";

// const router = Router();
// const ctrl = new TestProductController();

// router.post("/", ctrl.create);
// router.get("/", ctrl.getAll);
// router.get("/:id", ctrl.getOne);

// router.post("/:id/start", ctrl.start);
// router.post("/:id/stop", ctrl.stop);
// router.post("/:id/reboot", ctrl.reboot);
// router.post("/:id/force-shutdown", ctrl.forceShutdown);

// export default router;
